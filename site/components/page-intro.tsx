import content from '../content/portfolio';
import {asset} from '../lib/paths';
export function safeWebUrl(value?:string){if(!value)return undefined;try{const url=new URL(value);return ['https:','http:'].includes(url.protocol)?url.href:undefined;}catch{return undefined;}}
export function Cover({page}:{page:string}){const c=content.pages.find(p=>p.id===page);if(!c?.cover)return null;return <figure className="page-cover"><img src={asset(c.cover)} alt={c.coverAlt||''} style={{objectPosition:c.position||'center'}} width="1600" height="700"/>{c.credit&&<figcaption>{safeWebUrl(c.creditUrl)?<a href={safeWebUrl(c.creditUrl)} target="_blank" rel="noreferrer">{c.credit}</a>:c.credit}</figcaption>}</figure>}
export function PageIntro({page,eyebrow,title,description}:{page:string;eyebrow:string;title:string;description?:string}){return <><Cover page={page}/><div className="page-intro wrap"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1>{description&&<p className="page-description">{description}</p>}</div></>}
export function OrganizationMark({name}:{name:string}){const org=content.organizations.find(o=>o.name===name||o.match.some(m=>m&&name.toLowerCase().includes(m.toLowerCase())));const logo=org?.logo?<img className="organization-logo" src={asset(org.logo)} alt={org.name} loading="lazy" width="180" height="90"/>:null;return logo&&safeWebUrl(org?.url)?<a className="organization-logo-link" href={safeWebUrl(org?.url)} target="_blank" rel="noreferrer">{logo}</a>:logo}
